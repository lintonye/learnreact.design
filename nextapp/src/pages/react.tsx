import { createTagPostList } from '@/components/PostList'

const ReactPostList = createTagPostList({
  tagSlug: 'react',
  title: 'React',
  description: 'All posts related to React',
})
export default ReactPostList
